# Add Header

For each matched file, prepend a one-line comment at the top if no comment already exists. The comment should be `# auto-generated header` on its own line.

Do not modify files that already have a comment as their first line.
