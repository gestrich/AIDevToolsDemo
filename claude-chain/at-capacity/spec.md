# At Capacity

Keep these tasks pending so the scheduled trigger can detect the project but skip it because the configured capacity is zero.

## Tasks

- [ ] Create `output/capacity-1.txt` containing the single line: `This task should remain pending`
- [ ] Create `output/capacity-2.txt` containing the single line: `This task should also remain pending`
